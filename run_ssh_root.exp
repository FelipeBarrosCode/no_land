#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no -p 57906 -i ~/vastai root@184.144.255.144 "rm -f /usr/local/bin/noland-state-agent && systemctl stop noland-state-agent.service && rm -rf /run/noland/state-agent.sock"
expect {
    "Enter passphrase for key" {
        send "Casa8301\r"
        exp_continue
    }
    eof
}
