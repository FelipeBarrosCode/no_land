Project-managed GStreamer runtime payloads are staged here for packaged builds.

The build pipeline populates target-specific subdirectories such as:
- `x86_64-pc-windows-msvc/`

This file keeps the resource glob present even before a target runtime has been staged.
