[package]
name = "noland-mic-protocol"
version = "0.1.0"
edition = "2021"
description = "Noland microphone protocol — packet types, serialization, and authentication"

[dependencies]
bytes = "1.9"
ring = "0.17"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
