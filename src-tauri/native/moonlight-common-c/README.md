# moonlight-common-c placeholder

Phase 0 establishes the native build boundary and repository layout.

The production plan expects the upstream `moonlight-common-c` source tree to be pinned here as a git submodule before Phase 9+ work begins.
